# Local Studio bridge API (not used in RunPod worker image).
# This PC: install into .venv-studio (Python 3.14.6). Do not use the stale
# 3.14.0 .venv — ctypes breaks click/uvicorn after the 3.14.6 upgrade.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
requests>=2.32.0
python-multipart>=0.0.9
boto3>=1.35.0
