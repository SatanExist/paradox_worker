# План: сети / текстуры / инструменты юзера

> **Дата:** 2026-08-18  
> **Зачем:** приоритет Pedrokita — не Blender API, а то, что человек видит как 3D.  
> **Связь:** `postSideBackPlan.md`, `t2FinishPlan.md` (T2 freeze ✅), `textureWowPlan.md`, этот файл = очередь **наших** треков.

## Приоритет (замок)

| | |
|--|--|
| **▲ Высокий** | нейросети формы, текстурирование, инструменты в лабе/продукте |
| **▼ Низкий** | API для Blender / Unreal / MCP / «прочие приложения» |
| **Skip** | Draco в worker (локальный A/B можно не трогать) |
| **Запрет** | img2mv; крутилки T2 ради зада; MV-Adapter 80k на персонаже; Hunyuan Paint |

Сайт Studio — товарищ. Мы не верстаем.

## Где уже закрыто (не начинать заново)

| Тема | Статус |
|------|--------|
| T2 1-фото форма | потолок = ultra rt6; бок/зад mid |
| T2 персонаж tex | native PBR + W3a polish (High/Realistic) |
| W3b MVPainter | не next: W3a уже PASS |
| RVG как Meshy с 1 фото | не умеет; только реальные 2–4 вида |

## Два порядка (не путать)

**Список вслух (2026-08-16)** — как называли этапы:

```
lab ✅ → Draco skip → MCP last
→ MVPainter (tex) → RVG (реальные 2–4 фото) → Hi3DGen последним
❌ img2mv
```

**Почему я открыл Hi3DGen раньше пунктов 4–5:** не отмена списка, а **гейт**.  
MVPainter в спайке W3: не стартовать, пока W3a polish PASS (High≈Realistic).  
RVG: не жечь без реальных 2–4 фото / слотов.  
Hi3DGen в `t2FinishPlan`: открывать **после** «T2 сдали» — форма+tex в worker уже freeze.

Итого: вслух Hi3DGen **последний**; «сейчас» = первый **разблокированный** пункт из ▲ (сети), пока 4 и 5 стоят на гейтах.

Если гейты снять (гнать MVPainter всё равно / есть 2–4 фото) — возвращаемся к списку вслух, H0 пауза.

## H0 — зачем это первое

Дырка продукта: **львы / табард мыло в меше**, не в JPEG-постерах.  
Hi3DGen (Stable3DGen) = MIT, TRELLIS finetune + карта нормалей. Выход **геометрия** (~40 MB GLB), не native PBR.  
Сравниваем **форму** с T2, не «золотее».

Мастер спайка: `scripts/hi3dgen_h0_spike.md`.

## Критерий смены трека

| Если | Тогда |
|------|--------|
| H0 Side/Back/микро **лучше** T2 ultra глазами | H1 (как текстурировать этот меш) + U в лабе |
| H0 same/worse на рыцаре | закрыть метод; TripoSG только если явно просим |
| Нет реальных 2–4 фото | не открывать RVG |
| Сайт тормозит на High | тогда можно вспомнить Draco; не сейчас |

## Ops

- Pod: **terminate в той же сессии** (`activeContext` инцидент 2026-08-06).
- Dockerfile + CI → GHCR → volume → serverless (`workersMin=0`). Не голый `setup.sh`.
- `workersMin=0`. Не коммитить `.env`.
